import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      light: "#D0F1D0",
      main: "#91E094",
      dark: "#347834",
      contrastText: "#fff",
    },
    secondary: {
      light: "#E3F4E7",
      main: "#8FD09E",
      dark: "#32874C",
      contrastText: "#000",
    },
  },
});

export default theme;
